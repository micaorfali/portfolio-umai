import React from 'react';
import styles from '../Hero_5/Hero_5.module.css';

const Hero = () => {
  return (
    <section className={`hero is-large ${styles['container']}`}>
      <div className="hero-body"></div>
    </section>
  );
};

export default Hero;
