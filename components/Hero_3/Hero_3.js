import React from 'react';
import styles from '../Hero_3/Hero_3.module.css';

const Hero = () => {
  return (
    <section className={`hero is-large ${styles['container']}`}>
      <div className="hero-body">
      </div>
    </section>
  );
};

export default Hero;